import Head from 'next/head';
import Layout from '../layouts';

const Page = () => {
    return (
        <Layout layout="default">
            <Head>
                <title>Movie DB</title>
            </Head>

            <h1>test</h1>
        </Layout>
    );
};

export default Page;
